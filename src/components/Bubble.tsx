import { Bubble } from "react-chartjs-2";
import { Chart as ChartJS } from "chart.js/auto";

const Bubbles = () => {
    const options = {
        scales: {
            y: {
                beginAtZero: true,
            },
        },};
  return (
    <div>Buble place</div>
  )
}

export default Bubbles